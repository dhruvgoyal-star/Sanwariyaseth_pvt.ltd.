name: sanwariyaseth_app
description: Sanwariyaseth_pvt.ltd. - All-in-One + Social (Blue Theme)
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  provider: ^6.0.5
  firebase_core: ^2.12.0
  cloud_firestore: ^4.9.0
  firebase_auth: ^4.6.0
  firebase_storage: ^11.1.0
  image_picker: ^0.8.7+6
  cached_network_image: ^3.2.4
  url_launcher: ^6.1.10
  webview_flutter: ^4.7.0
  flutter_slidable: ^2.0.0
  carousel_slider: ^4.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png
